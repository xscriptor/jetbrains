<?xml version="1.0" encoding="UTF-8"?>
<idea-plugin>
  <id>com.xscriptor.theme</id>
  <name>Xscriptor Theme</name>
  <version>1.1.0</version>
  <vendor email="x@xscriptor.com" url="https://github.com/xscriptor">Xscriptor</vendor>
  
  <!-- Plugin icon -->
  <icon>pluginIcon.svg</icon>

  <description><![CDATA[
  A modern theme collection for Android Studio/IntelliJ with both dark and light variants. Features clean contrast, vivid accents, and a focused, minimal UI. Includes bundled editor color schemes for both themes.
  
  • Dark Theme: Deep blacks with purple/blue accents for comfortable coding in low-light environments
  • Light Theme: Clean whites with inverted color palette maintaining the same visual hierarchy
  • Both themes include matching editor color schemes optimized for syntax highlighting
]]></description>

<change-notes><![CDATA[
  1.1.0 — Migrated to Gradle 9 and the new IntelliJ Platform Gradle Plugin (2.x), updated target platform to 2025.3, configured Java 21 toolchain, and fixed ZIP packaging for IDE import. Broad product compatibility retained.
  1.0.1 — Added light theme variant with matching editor color scheme. Both dark and light themes available.
  1.0.0 — Initial release with full UI theme and editor scheme.
]]></change-notes>


  <!-- Compatibility with IntelliJ/Android Studio versions -->
  <idea-version since-build="211" until-build="253.*"/>

  <!-- Plugin dependencies -->
  <depends>com.intellij.modules.platform</depends>
  <depends>com.intellij.modules.json</depends>

  <!-- Plugin extensions -->
  <extensions defaultExtensionNs="com.intellij">
    <!-- Register light UI theme -->
    <themeProvider id="xscriptor.light.theme" path="/themes/xscriptor_light_theme.theme.json"/>
    
    <!-- Register dark UI theme (default) -->
    <themeProvider id="xscriptor.theme" path="/themes/xscriptor_theme.theme.json"/>
    
    <!-- Register light editor color scheme -->
    <bundledColorScheme path="/colors/Xscriptor Light Theme.xml"/>
    
    <!-- Register dark editor color scheme (default) -->
    <bundledColorScheme path="/colors/Xscriptor Theme.xml"/>
  </extensions>

  <!-- Plugin actions -->
  <actions>
    <!-- Action group for the theme -->
    <group id="XscriptorTheme.Actions" text="Xscriptor Theme" popup="true">
      <add-to-group group-id="ToolsMenu" anchor="last"/>
    </group>
  </actions>

</idea-plugin>
